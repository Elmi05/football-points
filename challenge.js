// Good Luck! You got this 💪🏾
// Write your code here.
function calculatePoints(wins, draws, losses) {
    return (wins * 3) + (draws * 1) + (losses * 0);
  }
  
  let teamPoints = calculatePoints(5, 2, 3);
  console.log(teamPoints);
  